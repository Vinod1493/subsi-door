spring:
  config:
    import: ""
  profiles:
    active: local
  cloud:
    config:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

eureka:
  client:
    enabled: false

jwt:
  secret: test-secret-key-for-unit-tests-only
  expiration: 86400000
